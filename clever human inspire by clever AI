#include<stdio.h>
int main(){
    int x;
    int mask1 = (1<<10) - 1 ;
    int mask2 = 0 ;
    for(int i = 0; i <  3 ; i++){
        scanf("%d",&x);
        mask2 = mask2|(1<<x);
    }
    mask1 = mask1-mask2;
    for(int i = 0 ; i < 10 ; i++){
        int flag;
        flag = (mask1>>i)&1;
        if(flag){
            printf("%d",i);
        }
    }
}
